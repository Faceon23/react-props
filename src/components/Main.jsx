import React from "react";


export default function Main({name, isAmazing}) {
    
    return (
        <>
        <h1>Hello {props.name}</h1>
        {isAmazing && <h2>Fruit: {fruit}</h2>}
        </>
        
    )
}

